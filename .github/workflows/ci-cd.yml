name: CI/CD - Deploy to Modal

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  PYTHON_VERSION: "3.11"
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  test:
    name: Test & Lint
    runs-on: ubuntu-latest
    env:
      # Validation-only default; tests use mocks and do not connect unless opted in.
      DATABASE_URL: postgresql://test:test@127.0.0.1:5432/postgres

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Cache pip packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/pyproject.toml') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint with ruff
        run: ruff check src/ tests/
        continue-on-error: true

      - name: Format check with black
        run: black --check src/ tests/
        continue-on-error: true

      - name: Run unit tests
        run: |
          pytest tests/unit/ -v --tb=short

      - name: Run integration tests
        run: |
          pytest tests/integration/ -v --tb=short

  deploy:
    name: Deploy to Modal
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    env:
      MODAL_TOKEN_ID: ${{ secrets.MODAL_TOKEN_ID || secrets.MODAL_TOKEN_ID || secrets.MODAL_AUTH_KEY }}
      MODAL_TOKEN_SECRET: ${{ secrets.MODAL_TOKEN_SECRET || secrets.MODAL_TOKEN_SECRET || secrets.MODAL_AUTH_SECRET }}
      MODAL_PROFILE: ${{ secrets.MODAL_API_PROFILE || vars.MODAL_API_PROFILE || 'vecinita' }}
      DATABASE_URL: ${{ secrets.DATABASE_URL || secrets.DB_URL }}
      VECINITA_EMBEDDING_API_URL: ${{ secrets.VECINITA_EMBEDDING_API_URL }}
      VECINITA_EMBEDDING_API_TOKEN: ${{ secrets.VECINITA_EMBEDDING_API_TOKEN }}

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Cache pip packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/pyproject.toml') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install modal

      - name: Install project
        run: pip install -e .

      - name: Deploy Workers App to Modal
        run: |
          PYTHONPATH=src python -m modal deploy src/vecinita_scraper/app.py

      - name: Deploy API App to Modal
        run: |
          PYTHONPATH=src python -m modal deploy src/vecinita_scraper/api/app.py

      - name: Deployment Complete
        run: |
          echo "✅ Successfully deployed vecinita-scraper to Modal"
          echo "   Workers: https://modal.com/apps/vecinita-scraper"
          echo "   API: https://modal.com/apps/vecinita-scraper-api"
